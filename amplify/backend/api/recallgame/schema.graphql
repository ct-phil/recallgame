type UserDetails @model @auth(rules: [{allow: public}]) {
  id: ID
  userSub: String!
  firstName: String!
  lastName: String!
  DOB: Int
  gender: String
  phoneNumber: Int
}

type UserScore @model @auth(rules: [{allow: public}]) {
  id: ID
  userSub: String!
  score: Int
} 