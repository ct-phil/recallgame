type UserDetails @model @auth(rules: [{allow: public}]) {
  id: ID
  userSub: String!
  firstName: String!
  lastName: String!
  DOB: String
  gender: String
  phoneNumber: String
}

type UserScore @model @auth(rules: [{allow: public}]) {
  id: ID
  userSub: String!
  score: Int
}
 